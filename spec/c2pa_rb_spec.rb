# frozen_string_literal: true

RSpec.describe C2paRb do
  it "has a version number" do
    expect(C2paRb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
